<!DOCTYPE html>
<html>
<head>
  <title>Overthink Bareng</title>
  <style>
    body {
      background: #0f0f1a;
      color: white;
      font-family: Arial;
      text-align: center;
    }

    textarea {
      width: 80%;
      height: 80px;
      border-radius: 10px;
      padding: 10px;
    }

    button {
      margin: 10px;
      padding: 10px 20px;
      border-radius: 10px;
      background: #6c5ce7;
      color: white;
      border: none;
    }

    .post {
      background: #1c1c2e;
      margin: 10px auto;
      padding: 10px;
      width: 80%;
      border-radius: 10px;
    }
  </style>
</head>

<body>

<h1>Overthink Bareng</h1>
<p>"Kalau kamu lagi mikir sesuatu... mungkin aku juga."</p>

<textarea id="input" placeholder="Apa yang lagi kamu pikirin?"></textarea><br>
<button onclick="kirim()">Kirim</button>

<div id="feed"></div>

<script>
let data = JSON.parse(localStorage.getItem("data")) || [];

function render() {
  let feed = document.getElementById("feed");
  feed.innerHTML = "";

  data.slice().reverse().forEach((item, i) => {
    let div = document.createElement("div");
    div.className = "post";

    div.innerHTML = `
      <p>${item.text}</p>
      <button onclick="relate(${i})">
        💭 ${item.relate} orang relate
      </button>
    `;

    feed.appendChild(div);
  });
}

function kirim() {
  let input = document.getElementById("input");

  if (!input.value) return;

  data.push({
    text: input.value,
    relate: 0
  });

  localStorage.setItem("data", JSON.stringify(data));
  input.value = "";
  render();
}

function relate(i) {
  data[i].relate++;
  localStorage.setItem("data", JSON.stringify(data));
  render();
}

render();
</script>

</body>
</html>